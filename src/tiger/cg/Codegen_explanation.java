package tiger.cg;

public class Codegen_explanation {
	/*
	 * 1.about the string constant center
	 * 		all started with _Sn ... (code stored in IcFun.string_center)
	 * 
	 * 2.the frame 
	 * 	svaed_regs(caller save),param n,param n-1,....,param 1,$ra,$access,$fp<fp's place>,
	 *  svaed_regs(callee save),var_l,var_outside_addr,var_tmp_spilled,...
	 */
}
